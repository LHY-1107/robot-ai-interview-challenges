# Problem Report & Troubleshooting

## Problem Background
机器人迎宾模块，需求：人员进入，等待指定延时之后输出问候；人员离开后状态重置；同一个人在场只问候一次。

## Potential Issues & Risk Points

### 1. 重复问候问题
风险：同一个人员，多次TICK事件，多次输出Hello。
根因：没有标记已经问候过的人员，每次TICK都判断时间差就输出。
解决方案：维护`greeted_person`集合，问候完成加入集合，TICK时跳过集合内人员。

### 2. 人员离开状态未清理
风险：人员已经离开，再次TICK依然输出问候；再次进入无法正常再次问候。
根因：PERSON_LEAVE事件到来，没有删除进入时间、没有清除已问候标记。
解决方案：收到PERSON_LEAVE，同时删除person_enter_time和greeted_person中的person_id。

### 3. TICK事件时间乱序
风险：TICK时间戳小于人员进入时间，时间差为负数，错误触发问候。
规避：业务假设输入事件时间戳时序合法；上层调用方保证时间递增。

### 4. 并发/多线程风险
风险：多线程同时调用handle_event，读写字典、集合会出现线程安全问题。
说明：当前实现为单线程模型；如果用于多线程环境，需要自行增加锁保护状态变量。

## Test Case Summary
1. 正常延时问候：人员进入，TICK时间到达延时阈值，输出问候。
2. 时间未到：TICK时间不足，无输出。
3. 在场重复TICK：只输出一次问候。
4. 离开再进入：离开清除状态，再次进入可以再次问候。
5. 未到延时直接离开：完全不会输出问候。

## Known Limitations
1. 状态全部保存在内存，程序重启全部丢失。
2. 不处理person_id为空的异常事件，依赖上层输入合法。
3. 不做持久化存储。
